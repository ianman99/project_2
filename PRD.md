# 사랑찾아 인생찾아 — PRD


| 항목    | 내용                                           |
| ----- | -------------------------------------------- |
| 문서 버전 | 1.0                                          |
| 작성일   | 2026-08-19                                   |
| 작성자   | 지인환 (6155)                                   |
| 상태    | **확정** — 11장 전 항목 결정 완료. OpenAI 모델 선택만 구현 시점으로 유보 |


---

## 1. 개요

### 1.1 제품 정의

**사랑찾아 인생찾아**는 LG전자 DX SCHOOL 6기 1반 수강생 24명이, 이미 수집된 상세 프로필을 바탕으로 AI가 분석한 "가장 잘 맞는 이성"을 근거와 함께 확인하는 폐쇄형 웹 서비스다.

### 1.2 해결하려는 문제

같은 반이지만 3차에 걸친 팀 편성에서 만난 사람은 일부에 불과하다. 예를 들어 6155는 11명과 팀을 했고 **12명과는 한 번도 같은 팀이 된 적이 없다**. 서로를 알 기회가 구조적으로 불균등하다.

### 1.3 핵심 가치

단순 점수가 아니라 **왜 잘 맞는지 근거**를 제시한다. 프로필에는 MBTI·취미·음식 취향·희망 직무·거주지·대화 소재·기피 요소까지 정규화돼 있어, AI가 사람이 납득할 수 있는 설명을 만들 수 있다.

### 1.4 대상 사용자

6기 1반 재학생 24명. **남 10명 / 여 14명.** 이성 매칭이므로 후보 풀은 남학생 기준 14명, 여학생 기준 10명이다.

---



## 2. 전제 및 제약


| #   | 전제                                                                              |
| --- | ------------------------------------------------------------------------------- |
| A1  | 폐쇄형 서비스. 학번 이메일 인증을 통과한 24명만 가입 가능                                              |
| A2  | 모수가 24명으로 매우 작다. 추천 알고리즘의 통계적 접근이 불가능하며, 전수 비교가 오히려 정확하다                        |
| A3  | **실존 인물의 개인정보**를 다룬다. 프라이버시가 최우선 제약이며 기능보다 우선한다                                 |
| A4  | 프로필 원본은 MongoDB Atlas `dxschool.students` 컬렉션 (24 문서). 서비스는 이 데이터를 읽기 전용으로 참조한다 |
| A5  | **이성 매칭만 지원한다.** 동성 매칭은 지원하지 않는 것으로 확정됐다 (Q7)                                   |


---



## 3. 범위



### 3.1 In Scope

- 학교 이메일 인증 기반 회원가입 / 로그인 / 로그아웃
- AI 이성 매칭 실행 및 결과 표시 (매칭률 %, 근거)
- 매칭 결과 영속화 — 재로그인 시 동일 결과 재열람
- 매칭 새로고침 (포인트 차감)
- 마이페이지 — 내 프로필, 포인트 잔액, 매칭 이력
- 포인트 충전 요청 (사용자) 및 수동 지급 (어드민)



### 3.2 Out of Scope (v1)

- 채팅 / 쪽지 / 연락처 교환 기능
- 상호 매칭(양쪽이 서로를 선택했을 때의 처리)
- 프로필 직접 수정 (원본 데이터가 스냅샷 기준)
- 결제 — 포인트는 서비스 내부 재화이며 현금 결제 없음
- 모바일 앱 (반응형 웹으로 대응)
- 동성 매칭

---



## 4. 페이지 명세

```
                    ┌─────────────┐
                    │  메인페이지  │ ← 비로그인 시 소개 + 로그인 유도
                    └──────┬──────┘
                           │
              ┌────────────┴────────────┐
              ▼                         ▼
       ┌─────────────┐          ┌──────────────┐
       │ 로그인페이지 │─────────▶│ 회원가입페이지 │
       └──────┬──────┘          └──────┬───────┘
              │                        │ 이메일 인증
              └────────┬───────────────┘
                       ▼
              ┌─────────────────┐      ┌─────────────┐
              │ 메인 (로그인 상태)│─────▶│  마이페이지  │
              │  = 매칭 화면     │      │ + 충전 요청  │
              └─────────────────┘      └──────┬──────┘
                                              │ 요청
                                              ▼
                                       ┌─────────────┐
                                       │ 어드민페이지 │ ← role: admin 전용
                                       │  포인트 지급 │
                                       └─────────────┘
```



### 4.1 메인페이지 `/`

서비스의 중심. 로그인 여부와 매칭 이력에 따라 **3가지 상태**를 가진다.

**상태 A — 비로그인**

- 서비스 소개, 대상 안내("6기 1반 전용")
- `로그인` / `회원가입` CTA

**상태 B — 로그인 + 매칭 이력 없음**

- 환영 문구 (이름 표시)
- `운명의 상대 찾기` 버튼 (주 CTA) — **1,000 P 차감**
- 버튼 근처에 현재 잔액과 차감액을 명시한다
- 클릭 → 차감 확인 → AI 매칭 실행 → 로딩 표시 → 결과 화면 전환

**상태 C — 로그인 + 매칭 이력 있음**

- 저장된 매칭 결과를 즉시 표시 (AI 재호출 없음)
- `운명의 상대 새로고침` 버튼 — **1,000 포인트 차감**
- 마지막 매칭 시각 표시
- 포인트 부족 시 버튼 비활성 + 사유 안내

**매칭 결과 표시 구성**


| 요소     | 설명                                      |
| ------ | --------------------------------------- |
| 운명의 상대 | **1위 후보 1명만** — 이름, 매칭률 %, 대표 한 줄       |
| 매칭 근거  | 왜 잘 맞는지 3~5개 항목 (예: "둘 다 크로스핏, 저녁형 인간") |
| 고려할 점  | 안 맞을 수 있는 부분 (예: "음식 취향이 갈림")           |
| 대화 소재  | 첫 대화용 추천 주제                             |

> **2위 이하는 화면에 노출하지 않는다.** AI는 후보 전원의 점수를 산출하고 DB에도 전원이 저장되지만(순위 근거 확보 및 향후 활용 목적), 사용자에게 보이는 것은 1위 1명뿐이다. API 응답에서도 1위만 내려보내 클라이언트에서 나머지가 드러나지 않게 한다.




### 4.2 로그인페이지 `/login`

- 입력: 학교 이메일 + 비밀번호
- `회원가입` 링크
- 실패 시 사유 구분 없이 동일 메시지 (계정 존재 여부 노출 방지)



### 4.3 회원가입페이지 `/signup`

**2단계 플로우**

```
[1단계] 이메일 입력
  → 형식 검증: <학번>@dxschool.co.kr
  → 학번이 24명 명단에 있는지 확인
  → 이미 가입된 학번인지 확인
  → 인증 메일 발송

[2단계] 인증코드 + 비밀번호 입력
  → 6자리 코드 검증 (10분 유효)
  → 비밀번호 설정
  → 계정 생성 + students 프로필과 연결
  → 초기 포인트 지급
```

**이메일 검증 규칙**

- 도메인은 `dxschool.co.kr` **고정**. 다른 도메인 전부 거부
- 로컬파트는 4자리 학번이며, `students` **컬렉션에 실제 존재하는** `_id`**여야 한다**
- 단순 정규식만으로는 부족하다. 명단 대조까지 해야 "유저 = 데이터" 일치가 보장된다



### 4.4 마이페이지 `/my`

- 내 프로필 요약 (students에서 조회)
- 포인트 잔액 및 사용 내역
- **포인트 충전 요청** — 사유 입력 후 어드민에게 요청, 처리 상태 표시
- 매칭 이력 — 과거 매칭 결과 열람
- 로그아웃 / 회원 탈퇴



### 4.5 어드민페이지 `/admin`

`role: "admin"` 계정만 접근 가능하다. 일반 사용자가 접근하면 404로 응답한다 (403은 페이지 존재를 노출한다).

- 대기 중인 충전 요청 목록 — 요청자, 사유, 요청 시각
- 요청별 `지급` / `거절` 처리 — 지급 시 금액 직접 입력
- 전체 사용자 포인트 잔액 목록
- 임의 사용자에게 직접 지급 (요청 없이도 가능)

---



## 5. 기능 요구사항



### F-1 회원가입 및 학교 이메일 인증


| ID    | 요구사항                                         | 우선순위   |
| ----- | -------------------------------------------- | ------ |
| F-1.1 | `<학번>@dxschool.co.kr` 형식만 허용하고 도메인 불일치는 거부한다 | Must   |
| F-1.2 | 학번이 `students`에 존재하지 않으면 가입을 거부한다            | Must   |
| F-1.3 | 6자리 숫자 인증코드를 발급한다 (전달 방식은 F-1.9)               | Must   |
| F-1.4 | 인증코드는 10분 후 만료된다                             | Must   |
| F-1.5 | 코드 검증 실패 5회 시 해당 요청을 폐기한다                    | Must   |
| F-1.6 | 재발송은 60초 쿨다운을 둔다                             | Should |
| F-1.7 | 이미 가입된 학번은 중복 가입을 거부한다                       | Must   |
| F-1.8 | 인증코드는 해시로 검증한다                              | Must   |
| F-1.9 | 메일 발송 수단이 없으면 어드민이 코드를 직접 발급·전달한다             | Must   |

**F-1.9 — 어드민 수동 발급**

배포 환경에서는 인증 메일을 보낼 수 없다.

- Render 무료 플랜은 아웃바운드 SMTP 포트(25·465·587)를 차단한다.
- `dxschool.co.kr`은 학교(Microsoft 365) 도메인이라 DNS를 건드릴 수 없어,
  발신 도메인 인증이 필요한 제3자 API(Brevo·Resend·SendGrid 등)도 쓸 수 없다.
  2024-02(Gmail·Yahoo), 2025-05(Microsoft)부터 도메인 인증이 사실상 의무다.

그래서 메일 설정(`BREVO_API_KEY` 또는 `SMTP_*`)이 하나도 없으면 발송을 시도하지 않고,
어드민 화면에 대기 중인 인증코드를 띄워 직접 전달한다.
메일이 설정돼 있으면 발송하되, 실패해도 가입을 막지 않고 어드민 발급으로 넘어간다.

이 때문에 인증코드를 평문으로도 보관한다. 어드민이 읽어서 전달해야 하기 때문이다.
만료 시각의 TTL 인덱스로 10분 뒤 자동 삭제되며, 검증 자체는 계속 해시로 한다.




### F-2 로그인 / 세션


| ID    | 요구사항                           | 우선순위 |
| ----- | ------------------------------ | ---- |
| F-2.1 | 이메일 + 비밀번호로 로그인한다              | Must |
| F-2.2 | 비밀번호는 bcrypt 등 단방향 해시로 저장한다    | Must |
| F-2.3 | 세션은 HttpOnly + Secure 쿠키로 관리한다 | Must |
| F-2.4 | 로그아웃 후 재로그인해도 매칭 결과가 유지된다      | Must |




### F-3 AI 매칭 실행 (핵심)


| ID    | 요구사항                                         | 우선순위   |
| ----- | -------------------------------------------- | ------ |
| F-3.1 | `운명의 상대 찾기` 클릭 시 본인 + 전체 이성 후보 데이터를 AI에 전달한다 | Must   |
| F-3.2 | 후보별 매칭률(0~100%)과 근거를 산출한다                    | Must   |
| F-3.3 | 결과를 구조화된 형식(JSON)으로 받아 검증 후 저장한다             | Must   |
| F-3.4 | AI 호출 실패 시 포인트를 차감하지 않는다                     | Must   |
| F-3.5 | 매칭 실행 중 중복 요청을 차단한다 (동시성 잠금)                 | Must   |
| F-3.6 | 실행 시간이 길 수 있으므로 진행 상태를 표시한다                  | Should |




### F-4 매칭 결과 영속화


| ID    | 요구사항                          | 우선순위   |
| ----- | ----------------------------- | ------ |
| F-4.1 | 매칭 결과를 DB에 저장한다               | Must   |
| F-4.2 | 재로그인 시 저장된 결과를 AI 재호출 없이 표시한다 | Must   |
| F-4.3 | 과거 매칭 이력을 보존한다 (덮어쓰지 않는다)     | Should |




### F-5 포인트 및 매칭 과금

**포인트 정책 (확정)**


| 항목      | 값                              |
| ------- | ------------------------------ |
| 가입 시 지급 | **5,000 P**                    |
| 최초 매칭   | **유료** — 1,000 P 차감            |
| 새로고침    | 1,000 P 차감                     |
| 추가 획득   | 사용자가 어드민에게 요청 → **어드민이 수동 지급** |
| 자동 충전   | 없음 (현금 결제 없음)                  |


> 가입 시 5,000P로 **총 5회** 매칭이 가능하다. 소진 후에는 어드민 지급이 유일한 경로다.


| ID    | 요구사항                                            | 우선순위 |
| ----- | ----------------------------------------------- | ---- |
| F-5.1 | 가입 완료 시 5,000 포인트를 지급한다                         | Must |
| F-5.2 | 최초 매칭과 새로고침 모두 1,000 포인트를 차감한다                  | Must |
| F-5.3 | 잔액 부족 시 실행을 차단하고 사유 및 충전 요청 경로를 안내한다            | Must |
| F-5.4 | 차감과 매칭 저장은 원자적으로 처리한다 — 한쪽만 성공하는 상태를 만들지 않는다    | Must |
| F-5.5 | 포인트 증감 내역을 거래 기록으로 남긴다                          | Must |
| F-5.6 | 매칭 이력이 있으면 `새로고침` 버튼을, 없으면 `운명의 상대 찾기` 버튼을 노출한다 | Must |




### F-6 마이페이지


| ID    | 요구사항                   | 우선순위   |
| ----- | ---------------------- | ------ |
| F-6.1 | 내 프로필 정보를 표시한다         | Must   |
| F-6.2 | 포인트 잔액과 사용 내역을 표시한다    | Must   |
| F-6.3 | 매칭 이력 목록과 상세를 열람할 수 있다 | Should |
| F-6.4 | 포인트 충전을 어드민에게 요청할 수 있다 | Must   |




### F-7 어드민 — 포인트 수동 지급


| ID    | 요구사항                            | 우선순위   |
| ----- | ------------------------------- | ------ |
| F-7.1 | 어드민 권한을 가진 계정만 어드민 화면에 접근할 수 있다 | Must   |
| F-7.2 | 대기 중인 충전 요청 목록을 확인할 수 있다        | Must   |
| F-7.3 | 특정 사용자에게 임의 금액의 포인트를 지급할 수 있다. **1회 지급 상한은 없다** (Q11) | Must   |
| F-7.4 | 지급 시 거래 기록에 어드민 ID와 사유를 남긴다     | Must   |
| F-7.5 | 요청을 거절 처리할 수 있다                 | Should |
| F-7.6 | 전체 사용자의 포인트 잔액을 조회할 수 있다        | Should |


> 어드민은 6155(서비스 운영자) 계정으로 시작한다. 권한은 `users.role` 필드로 판별하며, DB에서 직접 부여한다 — 앱에는 권한 승격 기능을 만들지 않는다.
>
> 지급 상한이 없으므로 오입력 방어는 UI가 맡는다. 지급 실행 전 금액과 대상을 확인하는 단계를 두고, 모든 지급은 `point_transactions`에 `grantedBy`와 `memo`로 기록되어 사후 추적이 가능하다.

---



## 6. 데이터 모델 (MongoDB)



### 6.1 `students` — 프로필 원본 (기존, 읽기 전용)

24개 문서. `_id`는 학번 문자열(`"6155"`). 서비스는 수정하지 않는다.

주요 필드: `profile`(성별·나이·MBTI·거주지), `career`, `interests`, `food`, `lifestyle`, `tmi`, `qna`, `teams`, `matching`, `bio`

> `matching` 블록은 이미 매칭용으로 정규화돼 있다 — `interest_keys`, `tags`, `dealbreakers`, `conversation_starters`, `food_avoid`, `never_teamed_with_ids` 등. **AI 입력의 핵심 소스로 쓴다.**



### 6.2 `users` — 계정 (신규)

```
{
  _id:          "6155",              // 학번 = students._id와 동일
  email:        "6155@dxschool.co.kr",
  passwordHash: "...",
  points:       5000,                // 가입 시 지급
  role:         "admin",             // "user" | "admin" — DB에서 직접 부여
  createdAt:    ISODate,
  lastLoginAt:  ISODate
}
```



### 6.3 `email_verifications` — 인증코드 (신규)

```
{
  _id:        ObjectId,
  studentNo:  "6155",
  email:      "6155@dxschool.co.kr",
  code:       "306129",              // 어드민 전달용 평문 (F-1.9). TTL로 10분 뒤 삭제
  codeHash:   "...",                 // 검증은 해시로
  expiresAt:  ISODate,               // TTL 인덱스로 자동 삭제
  attempts:   0,
  consumedAt: null,
  createdAt:  ISODate
}
```

`expiresAt`에 TTL 인덱스를 걸어 만료 문서를 자동 정리한다.

### 6.4 `matches` — 매칭 결과 (신규)

```
{
  _id:           ObjectId,
  userId:        "6155",
  generatedAt:   ISODate,
  isRefresh:     false,
  model:         "<사용 모델 ID>",
  promptVersion: "v1",
  results: [
    {
      candidateId: "6151",
      name:        "장소하",
      score:       87,               // 매칭률 %
      headline:    "운동과 저녁형 생활 리듬이 겹칩니다",
      reasons:     ["...", "..."],
      concerns:    ["..."],
      conversationStarters: ["..."]
    }
  ],
  usage: { inputTokens: 0, outputTokens: 0 }
}
```

`userId + generatedAt` 복합 인덱스. 최신 1건을 메인에 표시하고 나머지는 이력으로 보존한다.

### 6.5 `point_transactions` — 포인트 거래 (신규)

```
{
  _id:          ObjectId,
  userId:       "6155",
  delta:        -1000,
  reason:       "match_refresh",     // signup_bonus | match_initial
                                     // | match_refresh | admin_grant
  refId:        ObjectId,            // 관련 matches._id 또는 point_requests._id
  grantedBy:    null,                // admin_grant인 경우 어드민 학번
  memo:         null,                // 어드민 지급 사유
  balanceAfter: 2000,
  createdAt:    ISODate
}
```



### 6.6 `point_requests` — 포인트 충전 요청 (신규)

```
{
  _id:         ObjectId,
  userId:      "6151",
  message:     "새로고침 더 하고 싶어요",
  status:      "pending",            // pending | approved | rejected
  requestedAt: ISODate,
  resolvedAt:  null,
  resolvedBy:  null,                 // 처리한 어드민 학번
  grantedPoints: null                // 승인 시 실제 지급액
}
```

동일 사용자의 `pending` 요청은 1건으로 제한한다 (중복 요청 방지).

---



## 7. AI 매칭 설계



### 7.1 LLM 선택

**OpenAI 경량 모델을 사용한다** (구체적 모델 ID는 구현 시점에 확정 — 11장 Q8).

구조화 출력(Structured Outputs / JSON Schema)을 지원하는 모델이어야 한다. F-3.3이 검증 가능한 JSON을 요구하기 때문이다.

### 7.2 입력 데이터 설계

전체 프로필을 그대로 넣으면 낭비다. 실측 결과:


| 구성     | 문서당 평균      | 후보 14명 + 본인 (15건) |
| ------ | ----------- | ----------------- |
| 전체 문서  | 7,847 bytes | 약 115 KB          |
| 매칭용 트림 | 5,072 bytes | 약 74 KB           |


트림 대상에서 **반드시 제외할 필드**: `phone`(개인정보), `cohort`, `teams`의 상세 팀원 목록, `data_quality`, `_schema_version` 등 메타.

포함할 필드: `matching` 블록 전체, `bio`, `qna`, `tmi`, `food`, `interests`, `lifestyle`, `career`, 그리고 `profile`에서 성별·나이·MBTI·거주지만.

> 트림만으로 약 35% 절감된다. 한국어 JSON 기준 대략 **입력 25,000~35,000 토큰** 수준으로 추정하나, 한국어는 바이트당 토큰 비율이 영어보다 높으므로 구현 시 실제 토크나이저로 실측할 것.



### 7.3 출력 스키마

```json
{
  "results": [
    {
      "candidateId": "6151",
      "score": 87,
      "headline": "한 줄 요약",
      "reasons": ["근거1", "근거2", "근거3"],
      "concerns": ["고려할 점"],
      "conversationStarters": ["대화 소재"]
    }
  ]
}
```

- `score`는 0~100 정수
- 후보 전원에 대해 반환하고, 정렬은 서버가 수행한다
- 스키마 검증 실패 시 1회 재시도, 재실패 시 오류 처리 (포인트 차감 없음)
- **DB에는 전원을 저장하되, 클라이언트에는 1위만 응답한다** (Q3). 전원을 받는 이유는 1위 선정 근거를 확보하고 새로고침 시 결과 변화를 비교하기 위함이다



### 7.4 비용 구조

1회 매칭당 입력 약 3만 토큰, 출력 약 2~4천 토큰 규모다. 실제 단가는 선택 모델의 공식 가격표로 확인해야 한다.

**비용 절감 포인트 두 가지:**

1. **프롬프트 캐싱** — 후보 풀은 성별이 같은 사용자끼리 완전히 동일하다. 남학생 10명은 모두 같은 여성 후보 14명 데이터를 보낸다. 후보 블록을 프롬프트 앞쪽 고정 영역에 두면 캐시 적중이 가능하다.
2. **트림 프로젝션** — 7.2 참조.

> 매칭 1회당 1,000 포인트라는 과금은 이 실제 API 비용을 사용자에게 체감시키는 장치다. 무제한 재실행을 막는 것이 주 목적이며, 가입 시 지급되는 5,000 P는 총 5회 실행에 해당한다.



### 7.5 매칭 품질 고려사항

- **팀 이력 활용** — `never_teamed_with_ids`를 프롬프트에 포함하면 "아직 만날 기회가 없었던 사람"을 발굴하는 방향으로 유도할 수 있다. 서비스 취지(1.2절)와 직결된다
- **dealbreakers 반영** — 기피 항목이 충돌하면 감점 근거로 명시하게 한다
- **점수 인플레이션 방지** — 후보 전원이 85점 이상이면 변별력이 없다. 프롬프트에서 점수 분포를 넓히도록 지시한다

---



## 8. 이메일 발송 (네이버 SMTP)



### 8.1 설정


| 항목       | 값                           |
| -------- | --------------------------- |
| SMTP 호스트 | `smtp.naver.com`            |
| 포트       | 465 (SSL) 또는 587 (STARTTLS) |
| 인증       | 네이버 아이디 + 비밀번호              |
| 발신 주소    | 네이버 계정 이메일                  |




### 8.2 사전 준비 (사용자 작업)

1. 네이버 메일 → 환경설정 → **POP3/IMAP 설정에서 "사용함"으로 변경** (미설정 시 SMTP 인증 실패)
2. 2단계 인증 사용 중이면 **애플리케이션 비밀번호** 발급 필요



### 8.3 구현 시 필요한 정보

구현 단계에서 아래를 요청할 예정이다. **비밀번호는 채팅에 입력하지 말고** `be/.env`**에 직접 저장할 것.**


| #   | 필요 정보                      |
| --- | -------------------------- |
| 1   | 네이버 아이디 / 발신 이메일 주소        |
| 2   | SMTP 비밀번호 또는 애플리케이션 비밀번호   |
| 3   | 발신자 표시 이름 (예: "사랑찾아 인생찾아") |
| 4   | POP3/IMAP 설정 완료 여부         |




### 8.4 발송 제한

네이버 메일은 일일 발송량 제한이 있다. 24명 대상 서비스이므로 정상 사용에서는 문제가 없지만, 재발송 쿨다운(F-1.6)으로 남용을 방지한다.

---



## 9. 보안 및 프라이버시

이 서비스는 **실존 인물 24명의 개인정보**를 다룬다. 아래는 타협 대상이 아니다.


| #   | 원칙                                                                                        |
| --- | ----------------------------------------------------------------------------------------- |
| S-1 | **전화번호는 어떤 경로로도 다른 사용자에게 노출하지 않는다.** 프로필에 `phone` 필드가 존재하므로 AI 입력과 API 응답 양쪽에서 명시적으로 제외한다 |
| S-2 | 매칭 결과에 노출되는 상대 정보는 **이름, 나이, MBTI, 그리고 매칭 근거에 언급된 항목**으로 제한한다. 이 화이트리스트 밖의 필드는 응답에 포함하지 않는다 (Q5) |
| S-3 | 본인 프로필 외 타인의 원본 프로필을 조회하는 API를 제공하지 않는다                                                   |
| S-4 | 비밀번호와 인증코드는 해시 저장한다                                                                       |
| S-5 | 매칭 API는 본인 세션의 학번으로만 실행 가능하다 (타인 학번 지정 불가)                                                |
| S-6 | `.env`는 절대 커밋하지 않는다 (`.gitignore` 반영 완료)                                                  |
| S-7 | **매칭 결과는 본인만 열람한다.** 상대는 자신이 누군가의 운명의 상대로 선정됐는지 알 수 없으며, 이를 알려주는 API나 알림을 만들지 않는다 (Q9) |
| S-8 | 2위 이하 후보의 점수와 근거는 클라이언트로 내려보내지 않는다 (Q3). "누가 몇 위였는지"가 노출되면 관계에 해가 된다 |


> **데이터 사용 동의** — 24명 전원이 자신의 프로필을 타인의 매칭에 사용하는 것에 동의했다 (Q6).

---



## 10. 비기능 요구사항


| 항목       | 기준                              |
| -------- | ------------------------------- |
| 동시 사용자   | 최대 24명. 규모 대응은 불필요              |
| 매칭 응답 시간 | 30초 이내 목표. 초과 시 진행 표시 필수        |
| 페이지 로드   | 저장된 매칭 결과 표시는 1초 이내 (AI 재호출 없음) |
| 반응형      | 모바일 우선. 대부분 휴대폰으로 접속할 것으로 예상    |
| 브라우저     | 최신 Chrome / Safari              |


---



## 11. 확정 사항

### 11.1 결정 목록

모든 항목이 확정됐다. 아래 결정은 본문 각 절에 반영돼 있으며, 이 표는 결정 이력으로 남긴다.

| #   | 항목                       | 확정안                                        | 반영 위치 |
| --- | ------------------------ | ------------------------------------------ | ----- |
| Q1  | 포인트 정책                   | 가입 시 5,000 P, 최초 매칭도 유료(1,000 P), 추가 획득은 어드민 수동 지급 | F-5 |
| Q2  | `dxschool.co.kr` 도메인     | 실제 수신 가능한 도메인 — 이메일 인증 설계 유지               | 4.3, 8장 |
| Q3  | 매칭 결과 표시 범위              | **1위 1명만 보여준다.** 2위 이하는 노출하지 않는다           | 4.1, 7.3 |
| Q4  | 로그인 방식                   | 비밀번호 방식                                    | 4.2, F-2 |
| Q5  | 상대 프로필 공개 범위             | 이름, 나이, MBTI, 매칭 근거에 언급된 항목만               | S-2 |
| Q6  | 데이터 사용 동의                | **24명 전원 동의 받음**                           | 9장 |
| Q7  | 동성 매칭                    | 지원하지 않는다. 무조건 이성 매칭                        | A5, 3.2 |
| Q8  | OpenAI 모델                | **구현 시 확정** — 구조화 출력 지원이 선택 조건             | 7.1 |
| Q9  | 매칭 결과 공개 여부              | 비공개 — 본인만 열람. 상대는 자신이 선택됐는지 알 수 없다         | S-7 |
| Q10 | 충전 요청 경로                 | 앱 내 요청 (`point_requests` + 어드민 화면)         | 4.4, 4.5, F-7 |
| Q11 | 어드민 1회 지급 상한             | **상한 없음**                                  | F-7 |

### 11.2 구현 시점에 정할 것

**Q8 — OpenAI 모델 선택.** 유일하게 열려 있는 항목이다. 구조화 출력(Structured Outputs / JSON Schema)을 지원하는 경량 모델이어야 하며, 4단계(AI 매칭) 착수 시 모델 ID와 단가를 확정한다.


---



## 12. 구현 단계


11장이 전부 확정되어 선행 조건이 대부분 해소됐다. 바로 1단계부터 착수 가능하다.

| 단계    | 내용                                         | 선행 조건                       |
| ----- | ------------------------------------------ | --------------------------- |
| **1** | 백엔드 기반 — Express 서버, Mongo 연결, `be/src` 구성 | `be/src`가 현재 비어 있음          |
| **2** | 인증 — 회원가입, 이메일 인증(네이버 SMTP), 로그인, 세션       | 8.3 네이버 SMTP 정보 필요          |
| **3** | 프론트 골격 — 5개 페이지 라우팅, shadcn/ui 기반 레이아웃     | 완료된 Tailwind + shadcn 세팅 활용 |
| **4** | 포인트 — 가입 지급, 차감, 거래 기록                     | —                           |
| **5** | AI 매칭 — 프롬프트 설계, 구조화 출력, 결과 저장, 과금 연동      | Q8(모델 선택) 확정                |
| **6** | 마이페이지, 매칭 이력, 충전 요청                        | —                           |
| **7** | 어드민 — 요청 목록, 포인트 지급                        | —                           |
| **8** | 프라이버시 점검 (9장 S-1~S-8 전 항목 검증)              | 배포 전 필수                     |

> 포인트(4단계)를 매칭(5단계)보다 먼저 구현한다. 최초 매칭부터 과금이 걸리므로 매칭 기능이 포인트에 의존한다.


---



## 부록 A. 현재 구축 상태


| 항목            | 상태                                                              |
| ------------- | --------------------------------------------------------------- |
| MongoDB Atlas | 클러스터 `dxschool` (M0 FREE, AWS 서울) 운영 중                          |
| 프로필 데이터       | `dxschool.students` 24문서 적재 완료 (여 14 / 남 10)                    |
| 인덱스           | `_id_`, `profile.gender_1`, `name_1`                            |
| 백엔드           | `be` — Express 5, TypeScript, mongodb 드라이버 설치됨. `src` **비어 있음** |
| 프론트           | `fe` — React 19, Vite 8, Tailwind CSS v4, shadcn/ui 세팅 완료       |
| 환경변수          | `be/.env` (MONGODB_URI, MONGODB_DB). `.gitignore` 반영됨           |


> **주의 1** — 이 머신의 Node 기본 DNS 리졸버가 SRV 조회를 거부해서 `mongodb+srv://` 대신 표준 커넥션 스트링을 쓰고 있다. Atlas가 샤드 호스트명을 변경하면 `.env`를 수동 갱신해야 한다.
>
> **주의 2** — 원본 JSON 파일은 마이그레이션 후 프로젝트 밖으로 이동됐다. `be/scripts/migrate-profiles.js`를 재실행하려면 `DATA_DIR` 경로를 새 위치로 수정해야 한다. 현재 데이터의 원본은 MongoDB다.

